该作业用jupyter notebook编写而成。

该作业分成了两个模块：train.ipynb和test.ipynb.

classfication.pkl为train.ipynb训练好并保存下来的模型，因此无需再运行train.ipynb，直接运行test.ipynb即可。

为了压缩容量，Dataset文件夹被删去，助教还需将Assignment 2中的Dataset数据集放到本项目根目录下。
